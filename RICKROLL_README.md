# 🎵 macOS Rick Roll 惡作劇載荷

經典的 Rick Roll 惡作劇！完全無害，不會修改任何系統設定。

## 🎯 功能

插入 Digispark 後會自動：

1. 📢 **音量調到最大** (Shift+Option+F12)
2. ☀️ **亮度調到最大** (F2 × 20)
3. 🔍 **開啟 Spotlight** (Cmd+Space)
4. 🌐 **啟動 Safari**
5. 🎥 **開啟 YouTube Rick Roll 影片**
6. ▶️ **自動播放**
7. 🖥️ **進入全螢幕模式**

結果：Never Gonna Give You Up 全螢幕大聲播放 🎵

## 📊 編譯資訊

- **Flash**: 3400/8192 bytes (41.5%)
- **RAM**: 140/512 bytes (27.3%)
- **平台**: macOS 專用
- **影響**: 無系統修改，完全可逆

## 🚀 使用方法

### 1. 燒錄到 Digispark

```bash
cd ~/Developer/hw-018-keyboard-attack

# 拔掉 Digispark，然後執行：
SKETCH_NAME=rickroll_macos distrobox enter arduino-dev -- bash -c "
  sudo micronucleus --run /home/bs10081/Developer/hw-018-keyboard-attack/build/rickroll_macos.hex
"

# 看到提示後插入 Digispark
```

### 2. 測試

將燒錄好的 Digispark 插入 macOS 電腦，約 10 秒後：
- 音量和亮度會自動調整
- Safari 會自動開啟
- Rick Astley 會開始唱歌 🎤

## ⏱️ 時間軸

```
0s    - 插入 Digispark
1s    - 調整音量 (1秒)
2s    - 調整亮度 (1秒)
3s    - 開啟 Spotlight
4s    - 輸入 "Safari" 並啟動
7s    - Safari 啟動完成
8s    - 輸入 URL
13s   - 頁面載入
14s   - 自動播放
15s   - 全螢幕
```

## 🛡️ 安全性

✅ **完全安全**
- 不修改系統檔案
- 不安裝任何軟體
- 不讀取或傳送資料
- 只是開啟一個 YouTube 影片

✅ **容易復原**
- 按 Esc 退出全螢幕
- 關閉 Safari
- 調整音量和亮度回原狀

## 🎭 使用場景

- 愚人節惡作劇
- 朋友聚會娛樂
- 辦公室輕鬆時刻
- 資安意識教育展示

## 🔧 自訂修改

### 更換影片

編輯 `rickroll_macos.ino` 第 34 行：

```cpp
// 原本 (Rick Roll)
DigiKeyboard.println("https://www.youtube.com/watch?v=dQw4w9WgXcQ");

// 改成其他影片，例如：
DigiKeyboard.println("https://www.youtube.com/watch?v=YOUR_VIDEO_ID");
```

### 調整音量大小

修改第 13 行的循環次數：

```cpp
// 最大音量 (20次)
for(int i = 0; i < 20; i++)

// 中等音量 (10次)
for(int i = 0; i < 10; i++)

// 安靜模式 (0次，跳過音量調整)
// for(int i = 0; i < 0; i++)
```

### 取消全螢幕

註解掉第 45-49 行：

```cpp
// === 步驟 7：進入全螢幕 ===
// DigiKeyboard.sendKeyStroke(KEY_F, MOD_CONTROL_LEFT | MOD_GUI_LEFT);
// DigiKeyboard.delay(500);
// DigiKeyboard.sendKeyStroke(KEY_F);
```

## 🐛 故障排除

### Safari 沒有開啟
- 可能系統反應較慢，增加延遲時間
- 確認 Spotlight 搜尋正常運作

### 影片沒有播放
- 檢查網路連線
- YouTube 可能需要登入（某些地區）
- 增加頁面載入等待時間

### 沒有進入全螢幕
- macOS 版本不同，快捷鍵可能不同
- 手動按 `F` 鍵進入全螢幕

### 音量/亮度沒變化
- 某些 Mac 鍵盤配置不同
- 可能需要按 Fn 鍵組合

## 📝 程式碼說明

```cpp
// 音量調整：Shift+Option+F12 (直接跳到最大)
DigiKeyboard.sendKeyStroke(KEY_F12, MOD_SHIFT_LEFT | MOD_ALT_LEFT);

// 亮度調整：F2 (增加亮度)
DigiKeyboard.sendKeyStroke(KEY_F2);

// Spotlight：Cmd+Space
DigiKeyboard.sendKeyStroke(KEY_SPACE, MOD_GUI_LEFT);

// 網址列：Cmd+L
DigiKeyboard.sendKeyStroke(KEY_L, MOD_GUI_LEFT);

// 全螢幕：Ctrl+Cmd+F 或 F
DigiKeyboard.sendKeyStroke(KEY_F, MOD_CONTROL_LEFT | MOD_GUI_LEFT);
```

## 🎓 教育意義

這個載荷展示了：

1. **BadUSB 攻擊原理** - USB 裝置可以模擬鍵盤
2. **自動化腳本** - 無需使用者互動即可執行
3. **社交工程** - 看似無害的 USB 裝置
4. **物理安全重要性** - 不要隨便插入不明 USB

## ⚠️ 道德使用

- ✅ 在自己的電腦上測試
- ✅ 經過同意的朋友惡作劇
- ✅ 資安教育展示
- ❌ 未經授權使用
- ❌ 工作環境重要電腦
- ❌ 可能造成困擾的場合

## 🎉 彩蛋

如果你想讓惡作劇更完整，可以加入：

1. **隱藏 Dock** - 在全螢幕前執行
2. **停用通知** - 避免打斷播放
3. **循環播放** - 影片結束後重播

---

**Never gonna give you up, never gonna let you down! 🎵**

記得用於善意惡作劇，不要造成真正的困擾！
